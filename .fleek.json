{
  "build": {
    "image": "node:14",
    "command": "curl -sL https://unpkg.com/@pnpm/self-installer | node && pnpm i --store=node_modules/.pnpm && pnpm run auto:web:build",
    "publicDir": "web/build"
  }
}
